@* <div class="modal-overlay" style="@(_mostrar ? "display:flex;" : "display:none;")">
    <div class="modal-container">
        <button class="close-btn" @onclick="Cerrar">✖</button>
        <div class="modal-body">
            @ChildContent
        </div>
    </div>
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public EventCallback OnClose { get; set; }

    private bool _mostrar;

    public void Abrir()
    {
        _mostrar = true;
        StateHasChanged();
    }

    public async Task Cerrar()
    {
        _mostrar = false;
        await OnClose.InvokeAsync();
        StateHasChanged();
    }
}
 *@

<div class="modal-overlay" style="@(_mostrar ? "display:flex;" : "display:none;")">
    <div class="modal-container">
        <button class="close-btn" @onclick="Cerrar">✖</button>
        <div class="modal-body">
            @ChildContent
        </div>
    </div>
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public EventCallback OnClose { get; set; }

    private bool _mostrar;

    public void Abrir()
    {
        _mostrar = true;
        StateHasChanged();
    }

    public async Task Cerrar()
    {
        _mostrar = false;
        await OnClose.InvokeAsync();
        StateHasChanged();
    }
}
