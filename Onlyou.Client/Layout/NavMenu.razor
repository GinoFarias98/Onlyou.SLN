@using System.Security.Claims
@inject NavigationManager Navigation

<div class="navmenu-onlyou">
    <!-- Siempre visible -->
    <a class="nav-link" href="/" Match="NavLinkMatch.All">
        <i class="bi bi-house-door-fill"></i> Inicio
    </a>

    <!-- Menú condicional -->
    <AuthorizeView>
        <Authorized Context="authContext">
            <!-- Administrar Catálogo con submenú -->
            <div class="nav-item">
                <div class="nav-link catalog-toggle"
                     @onclick="ToggleCatalogMenu">
                    <div class="toggle-content">
                        <i class="bi bi-grid-1x2-fill"></i>
                        <span>Administrar Catálogo</span>
                        <i class="bi bi-chevron-@(showCatalogMenu ? "down" : "right") toggle-arrow @(showCatalogMenu ? "rotated" : "")"></i>
                    </div>
                </div>

                @if (showCatalogMenu)
                {
                    <div class="submenu">
 
                        <a class="submenu-link" href="productosV2" @onclick="CloseCatalogMenu">
                            <i class="bi bi-box-fill"></i> Productos
                        </a>
                        <a class="submenu-link" href="categorias" @onclick="CloseCatalogMenu">
                            <i class="bi bi-tags-fill"></i> Categorías
                        </a>
                        <a class="submenu-link" href="colores" @onclick="CloseCatalogMenu">
                            <i class="bi bi-palette-fill"></i> Colores
                        </a>
                        <a class="submenu-link" href="tipo-productos" @onclick="CloseCatalogMenu">
                            <i class="bi bi-box-seam"></i> Tipo Productos
                        </a>
                        <a class="submenu-link" href="talles" @onclick="CloseCatalogMenu">
                            <i class="bi bi-rulers"></i> Talles
                        </a>
                        <a class="submenu-link" href="marcas" @onclick="CloseCatalogMenu">
                            <i class="bi bi-star-fill"></i> Marcas
                        </a>
                        <a class="submenu-link" href="proveedores" @onclick="CloseCatalogMenu">
                            <i class="bi bi-people"></i> Proveedores
                        </a>
                    </div>
                }
            </div>

            <!-- Centro de Pedidos -->
            <a class="nav-link" href="Pedidos1">
                <i class="bi bi-cart-check-fill"></i> Centro de Pedidos
            </a>

            <!-- Reportes -->
            <a class="nav-link" href="movimientosCaja">
                <i class="bi bi-graph-up"></i> Reportes
            </a>

            <!-- === FOOTER === -->
            <div class="navmenu-footer">
                <a class="nav-link" href="Papelera">
                    <i class="bi bi-trash-fill"></i> Papelera
                </a>
            </div>

        </Authorized>

        <NotAuthorized>
            <!-- Puedes dejar vacío o agregar login si necesitas -->
        </NotAuthorized>
    </AuthorizeView>

    <!-- Siempre visible -->
    <!-- En tu NavMenu.razor -->
    <a href="http://localhost:5258/swagger" target="_blank" rel="noopener noreferrer" class="nav-link">
        <span class="oi oi-document" aria-hidden="true"></span> Swagger
    </a>




</div>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">

@code {
    private bool showCatalogMenu = false;

    private void ToggleCatalogMenu()
    {
        showCatalogMenu = !showCatalogMenu;
        StateHasChanged();
    }

    private void CloseCatalogMenu()
    {
        showCatalogMenu = false;
        StateHasChanged();
    }

    protected override void OnInitialized()
    {
        Navigation.LocationChanged += (sender, args) => CloseCatalogMenu();
    }
}